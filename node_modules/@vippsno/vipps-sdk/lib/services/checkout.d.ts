import * as types from '../@types';
export declare class Checkout {
    private headers;
    private checkoutSessionPath;
    private vippsHostname;
    constructor(configuration: types.InternalVippsConfiguration);
    createSession(requestData: types.Checkout.InitiateSessionRequest): Promise<types.Checkout.InitiateSessionResponse>;
    getSessionDetails(reference: string): Promise<types.Checkout.SessionResponse>;
}
